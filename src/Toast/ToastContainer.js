import './ToastContainer.css';
import Toast from './Toast';

const ToastContainer = () => {
  return (
    <div className="toast-container">
      <Toast message="Toast Test"/>
    </div>
  );
};

export default ToastContainer;
